/* Minimal stand-in for libxmp's internal format.h — see xmp.h in this dir.
 * The ProWizard sources include it through prowiz.h but use nothing from it. */
#ifndef REWAMP_PROWIZARD_FORMAT_H
#define REWAMP_PROWIZARD_FORMAT_H

/* Size of prowiz.c's pw_formats[] table (upstream libxmp format.h). */
#define NUM_PW_FORMATS 43

#endif
